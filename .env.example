# Only needed to push a new bundle to Hugging Face (scripts/push_submission_bundle.py).
HF_TOKEN=

# Override the model repo used by competition_transcriber.py and fetch_assets.sh.
# DOCENG_HF_REPO=radmada/lv-rover-mlt

# Override the Tesseract binary location if it is not on PATH.
# TESSERACT_CMD=
