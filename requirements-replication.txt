# Everything needed to regenerate the paper's numbers, not just run inference.
# Install on top of requirements.txt.
-r requirements.txt

# metrics and statistics
jiwer>=3.0
numpy>=1.23

# synthetic data generation
PyYAML>=6.0
fonttools>=4.40
datasets>=2.14

# corpus construction (experiments/corpus)
requests>=2.28
PyMuPDF>=1.23
pyrate-limiter>=3.0
pdf2image>=1.16

# cross-language runs (experiments/hungarian, experiments/luxembourgish)
pyphen>=0.14
pandas>=1.5

# tests
pytest>=7.0
